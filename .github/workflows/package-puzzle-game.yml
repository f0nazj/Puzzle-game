name: Package Puzzle Game

on:
  push:
    branches:
      - main
      - "主要"
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  macos:
    name: Build macOS DMG
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Build DMG
        run: |
          chmod +x build-mac.sh
          ./build-mac.sh

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: PuzzleGame-macOS
          path: build/package/*.dmg

  windows:
    name: Build Windows EXE
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Compile and package Windows app image
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"

          $project = (Get-Location).Path
          $src = Join-Path $project "gui"
          $build = Join-Path $project "build"
          $classes = Join-Path $build "classes"
          $dist = Join-Path $build "dist"
          $package = Join-Path $build "package"

          if (Test-Path $build) {
            Remove-Item $build -Recurse -Force
          }
          New-Item -ItemType Directory -Force -Path $classes, $dist, $package | Out-Null

          Get-ChildItem $src -Recurse -Filter "*.java" |
            ForEach-Object { $_.FullName } |
            Set-Content "$build\sources.txt" -Encoding UTF8

          javac -encoding UTF-8 -d $classes "@$build\sources.txt"

          New-Item -ItemType Directory -Force -Path "$classes\gui\ui" | Out-Null
          Copy-Item "$src\ui\images" "$classes\gui\ui\images" -Recurse

          @"
          Manifest-Version: 1.0
          Main-Class: gui.ui.App
          "@ | Set-Content "$build\MANIFEST.MF" -Encoding ASCII

          jar cfm "$dist\PuzzleGame.jar" "$build\MANIFEST.MF" -C $classes .

          jpackage `
            --input       $dist `
            --dest        $package `
            --name        "PuzzleGame" `
            --main-jar    "PuzzleGame.jar" `
            --main-class  "gui.ui.App" `
            --type        app-image `
            --app-version "1.0" `
            --vendor      "Student Project" `
            --win-console

          Compress-Archive -Path "$package\PuzzleGame" -DestinationPath "$package\PuzzleGame-windows.zip"

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: PuzzleGame-Windows
          path: build/package/PuzzleGame-windows.zip

  release:
    name: Publish GitHub Release
    needs:
      - macos
      - windows
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Download packaged apps
        uses: actions/download-artifact@v4
        with:
          path: release-files
          merge-multiple: true

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: release-files/*
          generate_release_notes: true
